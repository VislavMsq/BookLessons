package ArrayAndObj;

public class Second {
    // Field is an object variable of class First:
    First obj;
    // Class constructor:
    public Second(int n) {
        // Create an object and bind it with field - object variable
    obj = new First (n);
    obj.show();
        // Displaying the contents of the field - object:


    }
}
